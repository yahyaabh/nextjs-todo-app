import styles from "./Header.module.css";

function Header() {
  return (
    <div className={styles.cont}>
      <p className={styles.title}>simple to-do app enjoy!!</p>
    </div>
  );
}

export default Header;
